name: "Hello World"

on:
  push:
    branches: ["bootcamp-GitHub-Actions"]

jobs:
  deploy-helloWorld:
    runs-on: ubuntu-latest
    steps:
      run: echo "Hacemos pull del código"
      run: echo "Correr migraciones"
      run: echo "Generar binario"
      run: echo "Publicar version"